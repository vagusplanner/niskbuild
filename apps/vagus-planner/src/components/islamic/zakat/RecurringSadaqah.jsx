/**
 * Legacy Recurring Sadaqah UI — Stripe auto-pay is coming soon.
 * Recurring mode now lives in GivingPlan (local schedule + reminders).
 */
import GivingPlan from '@/components/zakat/GivingPlan';

export default function RecurringSadaqah() {
  return <GivingPlan />;
}
