/**
 * @deprecated Use ExternalCalendarManager — kept as an alias so any leftover
 * imports still render the single unified Calendar integrations card.
 */
export { default } from '@/components/integrations/ExternalCalendarManager';
