{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "user"
      ],
      "default": "user"
    },
    "photo_url": {
      "type": "string",
      "description": "URL to the user's profile photo"
    }
  }
}